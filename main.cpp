/**
 * @file main.cpp
 * A simple C++ program that manipulates an image in a variety of ways.
 */

#include "engine.h"

int main() {
    Engine gameEngine = Engine();
    gameEngine.runEngine(2);
    return 0;
}
